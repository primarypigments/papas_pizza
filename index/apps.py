from django.apps import AppConfig


class IndexConfig(AppConfig):
    """
    Configuration for the Index application with default settings.
    """
    name = 'index'
    default_auto_field = 'django.db.models.BigAutoField'
